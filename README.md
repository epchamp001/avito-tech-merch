# Avito Merch Store 🚀

## Задание 🎯
В Авито существует внутренний магазин мерча, где сотрудники могут приобретать товары за монеты (coin). Каждому новому сотруднику выделяется 1000 монет, которые можно использовать для покупки товаров. Кроме того, монеты можно передавать другим сотрудникам в знак благодарности или как подарок.

Необходимо реализовать сервис, который позволит сотрудникам обмениваться монетками и приобретать на них мерч. Каждый сотрудник должен иметь возможность видеть:

Список купленных им мерчовых товаров
Сгруппированную информацию о перемещении монеток в его кошельке, включая:
Кто ему передавал монетки и в каком количестве
Кому сотрудник передавал монетки и в каком количестве
Количество монеток не может быть отрицательным, запрещено уходить в минус при операциях с монетками.

Мерч — это продукт, который можно купить за монетки. Всего в магазине доступно 10 видов мерча. Каждый товар имеет уникальное название и цену.
Предполагается, что в магазине бесконечный запас каждого вида мерча.

## Запуск проекта 🏁

Для развёртывания всех сервисов (приложение, база данных, pgAdmin, Prometheus, Grafana) используйте команду:
 
```bash
 docker compose up -d
 ```

Эта команда поднимет все контейнеры в фоновом режиме.

### Доступные сервисы после успешного запуска

| Сервис       | Описание                                                                 | Доступно на                          | Порт  | Дополнительная информация                                                                 |
|--------------|-------------------------------------------------------------------------|--------------------------------------|-------|------------------------------------------------------------------------------------------|
| **app**      | Основное приложение (Merch Store).                                      | `http://localhost:8080`              | 8080  | Зависит от PostgreSQL. Переменные окружения настроены для подключения к базе данных и JWT. |
| **Swagger**  | Документация API приложения.                                            | `http://localhost:8080/swagger/index.html` | 8080  | Доступно через основное приложение.                                                      |
| **pgAdmin**  | Веб-интерфейс для управления PostgreSQL.                                | `http://localhost:5050`              | 5050  | Учётные данные: `epchamp001@gmail.com` / `123champ123`.                                   |
| **Grafana**  | Сервис для визуализации метрик.                                         | `http://localhost:3000`              | 3000  | Учётные данные: `champ001` / `123champ123`. Зависит от Prometheus.                       |

## Запуск тестов 🧪

### Запуск Unit-тестов

Для запуска всех unit-тестов выполните команду:

```bash
go test ./...
```

### Запуск интеграционных тестов

Для запуска как unit-тестов, так и интеграционных тестов, помеченных тегом integration, используйте:

```bash
go test ./... --tags=integration
```

## Остановка всех сервисов 🛑

Чтобы остановить и удалить все контейнеры, используйте команду:

```bash
docker compose down
```

Эта команда остановит все запущенные сервисы и удалит созданные контейнеры, а также сети, созданные Docker Compose.

## Реализация 🔧

### Схема базы данных 🗃️

![ERD-merch-store](https://github.com/user-attachments/assets/15f70d24-5c8e-4eca-9108-7dc310e604a3)

### Использование pgxpool и реализация репозитория

#### Выбор pgxpool 💡
В данном проекте для работы с PostgreSQL используется pgxpool, что обусловлено следующими преимуществами:

* **Производительность и масштабируемость:** pgxpool позволяет эффективно управлять пулом соединений, что критично при большом количестве одновременных запросов.
* **Поддержка современных возможностей PostgreSQL:** Благодаря поддержке новейших функций PostgreSQL, таких как расширенные уровни изоляции транзакций, pgxpool обеспечивает надежное и гибкое взаимодействие с базой данных.
* **Простота интеграции:** Удобный API pgxpool позволяет легко интегрировать его в архитектуру приложения и абстрагировать детали работы с базой данных.

#### Архитектура репозитория 🗂️
Для разделения доступа к данным и бизнес-логики реализован интерфейс репозитория.
Это позволяет:
* **Изолировать бизнес-логику от деталей работы с базой данных:** Взаимодействие с данными происходит через набор интерфейсов, что упрощает тестирование и поддержку кода.
* **Объединить различные сущности в единое целое:** Интерфейс Repository объединяет работу с пользователями, мерчем, покупками и транзакциями:

```go
type Repository interface {
    UserRepository
    MerchRepository
    PurchaseRepository
    TransactionRepository
}
```

Каждый из вложенных интерфейсов отвечает за свою область:

* **UserRepository:** создание, обновление, получение информации о пользователях и их балансе.
* **MerchRepository:** получение списка мерча и данных по конкретному товару.
* **PurchaseRepository:** создание и получение информации о покупках.
* **TransactionRepository:** создание и получение данных о транзакциях с монетками.

#### Реализация транзакций 🔄
Для обеспечения целостности данных при операциях, связанных с изменением баланса, покупками и переводом монет, реализована поддержка транзакций через TxManager. 

Основные моменты:

* **Абстракция транзакций:** Интерфейс TxManager предоставляет метод WithTx, который принимает функцию с операциями внутри транзакции. Это позволяет гарантировать, что все операции либо выполнятся полностью, либо в случае ошибки будут отменены.
* **Уровни изоляции и режимы доступа:** При выполнении транзакций используется возможность задания уровня изоляции (например, pgx.Serializable, pgx.ReadCommitted, pgx.RepeatableRead) и режима доступа (чтение/запись). Это помогает избежать ситуаций гонок и обеспечивает корректное поведение при параллельном доступе к данным.
* **Реализация интерфейса Executor:** Одним из ключевых преимуществ является то, что как pgxpool.Tx, так и pgxpool.Pool реализуют интерфейс Executor. Это позволило унифицировать методы работы с базой данных в рамках TxManager, поскольку и транзакционные, и нетранзакционные операции используют единый набор методов для выполнения SQL-запросов.
* **Чистая архитектура:** Логика работы с базой данных вынесена в отдельный слой (репозитории и менеджер транзакций). Такой подход позволяет бизнес-логике оставаться независимой от конкретной реализации доступа к данным, что упрощает поддержку, тестирование и масштабирование приложения.

В таком подходе бизнес-логика приложения работает через интерфейсы, а реализация конкретного доступа к данным полностью инкапсулирована в репозиториях и менеджере транзакций, что соответствует принципам чистой архитектуры.

### Выбор логгера: zap 🔥

#### Почему zap?

Для логирования в проекте выбран zap по следующим причинам:

* **Высокая производительность:** zap оптимизирован для минимизации накладных расходов на логирование, что особенно важно для высоконагруженных приложений.
* **Структурированное логирование:** zap позволяет выводить логи в структурированном виде, что облегчает их анализ и последующую агрегацию.
* **Гибкая настройка:** zap поддерживает различные режимы работы (например, режим разработки и продакшн), что позволяет адаптировать логирование под конкретные потребности окружения.

#### Особенности реализации

В реализации логгера использован следующий подход:

**Абстракция через интерфейс:** Определён интерфейс Logger, который включает базовые методы логирования с поддержкой как обычного, так и контекстного вывода (с ключами и значениями). Это позволяет в дальнейшем легко заменить реализацию логгера без изменения бизнес-логики.

**Конфигурация для разных режимов:** Функция NewLogger(mode string) принимает параметр режима (например, "dev" или "prod").

**Использование SugaredLogger:** Для удобства работы с логами реализованы методы, принимающие неструктурированные данные (Infow, Debugw и т.д.), что упрощает запись логов с произвольными ключами и значениями.

Благодаря интерфейсу **Logger** весь проект использует единый способ логирования, что упрощает поддержку и масштабирование кода.

### Маршруты API и аутентификация 🔐

В проекте используется Gin для маршрутизации HTTP-запросов. Основной файл настройки маршрутов можно найти в `internal/app/routes.go`

#### Описание маршрутов

| **Endpoint**                                           | **Описание**                                                                                                                                                                                                                                                                                                   | **Примечания**                                                                                                                                                                                         |
|--------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **/api/auth/register**                                 | Регистрация новых пользователей. Клиент отправляет необходимые данные, и система создаёт учетную запись.                                                                                                                                                                                                    | —                                                                                                                                                                                                      |
| **/api/auth/login**                                    | Аутентификация пользователей. Несмотря на то, что задание подразумевает автоматическое создание пользователя при первом входе, логика разделена на отдельный маршрут.                                                                                                                                      | - Явное разделение ответственности: упрощает контроль аутентификации и валидацию данных.<br>- Повышение безопасности: изолирует процессы регистрации и логина.<br>- Удобство тестирования и поддержки. |
| **/api/info, /api/send-coin, /api/merch, /api/merch/buy/:item** | Доступ к операциям (просмотр информации, перевод монет, получение списка мерча и покупка мерча).                                                                                                                                                                                                               | Защищены JWT-middleware: доступны только авторизованным пользователям.                                                                                                                                 |
| **/swagger/**                                          | Доступ к документации API, сгенерированной с помощью `swaggo/swag`.                                                                                                                                                                                                                                             | —                                                                                                                                                                                                      |


### Документация API с помощью Swagger 📚

Для документирования API в проекте используется `swaggo/swag`. Основная конфигурация описана в файлах, которые находятся в `docs`.

Документация доступна через веб-интерфейс Swagger UI, который интегрирован в проект. Эндпоинт для просмотра документации настроен по адресу:

```
localhost:8080/swagger/index.html
```

**Swagger UI**

![swagger-ui](https://github.com/user-attachments/assets/c22f4d37-3d9a-4f69-aa7b-5fc94cbd27c7)

### Тестирование 🧪

#### Unit-тестирование 

В этом проекте большое внимание уделено качественному тестированию. Были применены различные подходы для проверки работы ключевых компонентов, что позволило обеспечить высокое покрытие бизнес-логики и контроллеров.

Для имитации зависимостей и проверки поведения сервисов моки генерируются с помощью `mockery`. Это позволяет изолировать тестируемые функции от внешних зависимостей.

Полностью покрыт слой бизнес-логики и контроллеров. Каждый важный сценарий работы реализован в отдельных тестах, что облегчает поддержку и выявление ошибок.
Также были написаны тесты для функций хэширования паролей.

Для одного из методов `MerchService` был реализован тест с использованием подхода `Table-Driven`. Несмотря на то, что этот подход позволяет компактно описывать набор тестовых кейсов, он был применён только для одного теста, так как предпочтение отдано более явному разделению тестов по функциям.

Большинство тестов написаны как отдельные функции, , что обеспечивает наглядность и удобство отладки.

**Проценты покрытия тестами:**

```bash
controller: ok      avito-tech-merch/internal/controller/http       0.335s  coverage: 95.6% of statements
service: ok      avito-tech-merch/internal/service       0.795s  coverage: 98.0% of statements
models/dto: ok      avito-tech-merch/internal/models/dto    0.308s  coverage: 100.0% of statements
pkg/password: ok      avito-tech-merch/pkg/password   0.684s  coverage: 83.3% of statements
```

Подробнее про покрытие каждого метода можно посмотреть с помощью уже сгенерированного файла `coverage/coverage.html`

### Интеграционное тестирование

В проекте интеграционные тесты реализованы с использованием тестового фреймворка `testify/suite` и запуском контейнера PostgreSQL для имитации реального окружения базы данных. Тесты помечены **build-тегом** `integration`, что позволяет запускать их отдельно.

В методе `SetupSuite` создается тестовый контейнер PostgreSQL, выполняются миграции, настраивается пул соединений с использованием pgxpool, и формируется полная конфигурация приложения. На основе этой конфигурации инициализируются репозитории, сервисы и контроллеры, после чего создается HTTP-сервер с маршрутизацией через Gin. Это обеспечивает максимально реалистичное окружение.

В методе `SetupTest` происходит очистка таблиц и сброс идентификаторов, что гарантирует изоляцию тестовых сценариев и отсутствие влияния предыдущих тестов на последующие.

Метод `TearDownSuite` отвечает за корректное завершение работы тестового контейнера и закрытие HTTP-сервера.

Тесты выполняют реальные HTTP-запросы к развернутому серверу, что позволяет протестировать всю цепочку обработки запроса – от контроллеров до базы данных.

Для проверки корректности работы транзакций в методе передачи монет я реализовал специальный механизм "тригерного" значения. В коде передачи монет добавлен флаг, который активируется при выполнении определённого условия:

```go
flag := false
if senderBalance == 666 && os.Getenv("TEST_ENV") == "true" {
    flag = true
}
```

При установленном флаге происходит следующее:

* Сначала выполняется update для первого пользователя.
* Затем, при выполнении второго update, если flag равен true, вызывается искусственная ошибка (просто return err), что приводит к срабатыванию механизма отката (rollback).

Таким образом, можно убедиться, что при возникновении ошибки в середине транзакции все изменения отменяются, и состояние базы данных остается неизменным.

Стоит отметить, что этот тест был проведён для проверки работы транзакций, но позже был удалён, поскольку он внедрялся непосредственно в метод бизнес-логики. Тем не менее, данный эксперимент позволил убедиться в корректном функционировании механизма транзакций и отката в случае ошибок.

Интеграционные тесты в проекте позволяют проверить корректность работы всего стека – от контроллеров до базы данных. Благодаря использованию тестового контейнера PostgreSQL и реалистичному окружению, тесты выявляют ошибки, которые могли бы остаться незамеченными при юнит-тестировании, и обеспечивают уверенность в стабильной работе приложения в боевых условиях.
При этом интеграционные тесты охватывают все доступные маршруты сервиса, что позволяет гарантировать работоспособность каждого метода бизнес-логики.

### Мониторинг с помощью Prometheus 📈

В проекте реализована система мониторинга, которая собирает метрики, связанные с HTTP-запросами, базой данных, бизнес-логикой и ресурсами.

Все метрики определены в пакете `internal/metrics` и включают следующие показатели:

#### 📌 HTTP-запросы

| Метрика               | Тип       | Описание                                                                 |
|-----------------------|-----------|-------------------------------------------------------------------------|
| `HTTPRequestsTotal`   | Счётчик   | Общее количество HTTP-запросов, разбитое по меткам метода, эндпоинта и статусу ответа. |
| `HTTPRequestDuration` | Гистограмма | Время выполнения HTTP-запросов. Позволяет оценить производительность и время отклика. |
| `HTTPErrorsTotal`     | Счётчик   | Количество ошибок HTTP-запросов. Помогает отслеживать сбои в обработке запросов. |

#### 📌 Запросы к базе данных

| Метрика               | Тип       | Описание                                                                 |
|-----------------------|-----------|-------------------------------------------------------------------------|
| `DBQueryDuration`     | Гистограмма | Время выполнения запросов к базе данных. Помогает определять "узкие места" в работе с данными. |
| `DBActiveConnections` | Gauge     | Количество активных соединений с базой данных.                          |
| `DBErrorsTotal`       | Счётчик   | Количество ошибок, возникающих при выполнении запросов к базе данных.   |

#### 📌 Бизнес-логика

| Метрика               | Тип       | Описание                                                                 |
|-----------------------|-----------|-------------------------------------------------------------------------|
| `RegistrationsTotal`  | Счётчик   | Количество регистраций новых пользователей.                             |
| `LoginsTotal`         | Счётчик   | Количество успешных авторизаций.                                        |
| `CoinTransfersTotal`  | Счётчик   | Количество операций по передаче монет между пользователями.             |
| `MerchPurchasesTotal` | Счётчик   | Количество покупок мерча.                                               |

Все эти метрики визуализируются в Grafana в dashboard Merch Store Metrics, что позволяет оперативно отслеживать состояние сервиса, анализировать производительность и выявлять возможные проблемы.

### Нагрузочное тестирование с использованием k6 ⚙️

Скрипты для нагрузочного тестирования находятся в директории `scripts/k6` (файл `load_tests.js`). Они позволяют моделировать реальную работу сервиса под нагрузкой, эмулируя поведение пользователей.

Скрипт выполняет следующие шаги:

1. Регистрация
2. Логин
3. Получение информации
4. Передача монет
5. Получение списка мерча
6. Покупка мерча

#### Замечания по производительности

В текущей реализации скорость ответа не соответствует желаемой. Это связано с использованием транзакций с уровнем изоляции serializable в операциях покупки мерча и перевода монет. Такой уровень гарантирует целостность данных, но значительно замедляет работу сервиса.

Иногда при переводе монет возникает ошибка с кодом `sqlstate 40001`. Для её обработки реализован механизм повторных попыток транзакции (до трёх раз). Если после повторений операция всё равно завершается неудачно, возвращается ошибка. Такое повторение снижает общее время ответа, поэтому успешными проходят лишь около 97% запросов на перевод монет, а остальные завершаются с ошибкой 40001. Но мне кажется, что проблема в самом тесте, так как несколько пользователей пытаются перевести монеты одному и тому же человеку.

![res k6](https://github.com/user-attachments/assets/97ab1d6f-aa5a-497d-a302-85cfc37b03b4)

Метрики, собираемые скриптом `k6`, отправляются в Prometheus по `remote write`. Все данные визуализируются в Grafana на dashboard`е **k6 Prometheus (Native Histograms)**.

Результаты нагрузочного тестирование с помощью `k6`:

![k6](https://github.com/user-attachments/assets/a47d9fbb-eed2-410b-8ca1-2e61d0ad978d)

### Визуализация метрик с Grafana 🖼️

Для мониторинга и анализа метрик проекта используется **Grafana**. Контейнер Grafana настроен в `docker-compose`.

**Адрес:** Grafana доступна по адресу http://localhost:3000

**Учётные данные для входа:**
* **Пользователь:** champ001
* **Пароль:** 123champ123

#### Dashboards

В Grafana настроено два основных dashboard`a для визуализации метрик проекта:

**Merch Store Metrics** - этот dashboard собирает метрики, связанные с HTTP-запросами, Запросами к базе данных, Операциями бизнес-логики.

![Merch Store Metrics](https://github.com/user-attachments/assets/6f7328ae-e0a7-4a70-a9eb-13f5f99c587c)

**k6 Prometheus (Native Histograms)** - данный dashboard визуализирует метрики, собранные в ходе нагрузочного тестирования с использованием `k6`.

![k6 Prometheus](https://github.com/user-attachments/assets/14d9c8ec-c3fb-41eb-8600-1bdc1d93bb06)

### Graceful Shutdown 🛑

Для обеспечения корректного завершения работы приложения реализован механизм **graceful shutdown**. Он позволяет корректно завершить работу HTTP-сервера, метрик и закрыть соединения с базой данных, гарантируя, что все запущенные процессы будут завершены до остановки приложения.

В функции `main` создается контекст, который слушает сигналы `os.Interrupt`, `SIGTERM` и `SIGQUIT`. При получении любого из этих сигналов происходит отмена контекста:

```go
ctx, stop := signal.NotifyContext(context.Background(),
    os.Interrupt, syscall.SIGTERM, syscall.SIGQUIT)
defer stop()
```

Создается экземпляр сервера через функцию `NewServer`, где происходит инициализация маршрутов, подключения к базе данных, запуск HTTP-сервера и сервера для метрик. Метод `server.Run(ctx)` запускает два горутины для обработки запросов – один для основного HTTP-сервера, другой для сервера метрик. Также в отдельной горутине собираются метрики активных соединений с базой данных.

После получения сигнала завершения происходит ожидание отмены основного контекста (`<-ctx.Done())`. Далее создается новый контекст с таймаутом (значение берется из конфигурации), в течение которого сервер должен завершить свою работу. В методе `server.Shutdown(shutdownCtx)` происходит последовательное закрытие всех компонентов.

Для упрощения процесса завершения работы реализован тип `Closer`, который позволяет аккуратно закрыть все зарегистрированные функции в обратном порядке (LIFO). В функции `NewServer` с помощью `closer.Add()` добавляются функции для закрытия PostgreSQL пула, HTTP-сервера и сервера метрик. Метод `Shutdown(ctx)` вызывает метод `Close(ctx)` у объекта `Closer`, который параллельно выполняет все функции закрытия и собирает возможные ошибки.

Благодаря graceful shutdown, сервер может корректно завершить обработку текущих запросов, закрыть соединения с базой данных и остановить сбор метрик.

Механизм graceful shutdown обеспечивает стабильное и предсказуемое завершение работы приложения, позволяя избежать потери данных и обеспечивая корректное закрытие всех компонентов сервиса.

### Dockerfile и Docker Compose 🐳

#### Dockerfile

Dockerfile реализует multi-stage сборку приложения

#### Docker Compose

Docker Compose используется для оркестрации нескольких сервисов приложения, обеспечивая удобное управление зависимостями и настройку сети.

Основные сервисы:

| Сервис     | Описание                                                                 | Зависимости                          | Переменные окружения / Параметры                     | Volumes                     |
|------------|-------------------------------------------------------------------------|--------------------------------------|-----------------------------------------------------|----------------------------|
| **app**    | Собирается из Dockerfile, запускает приложение.                         | Зависит от `postgres` (с проверкой состояния). Работает в сети `merch-store`. | Переменные для подключения к PostgreSQL и конфигурации JWT. | -                          |
| **postgres** | Используется официальный образ PostgreSQL.                             | -                                    | Параметры базы данных: имя, пользователь, пароль.    | `merch-store-data` (для хранения данных). |
| **pgadmin** | Веб-интерфейс для управления PostgreSQL.                              | -                                    | Учётные данные для входа.                            | Volume для хранения данных. |
| **prometheus** | Сервис для сбора метрик. Используется официальный образ Prometheus. | -                                    | Локальный файл конфигурации `prometheus.yml`.        | -                          |
| **grafana** | Сервис для визуализации метрик.                                       | Зависит от `prometheus`.             | Учётные данные для входа.                            | Volume для хранения данных и директория `provisioning` для дашбордов. |

### Настройка линтера (.golangci.yaml) 👨🏼‍💻
Для поддержания высокого качества кода в проекте используется **golangci-lint**. Конфигурация линтера описана в файле `.golangci.yaml`.

### Что можно сделать еще? 🔧🚀

Можно интегрировать **Redis** для кэширования критичных данных. Например, хранить в **Redis**:
* `username` и `balance` пользователей
* Информацию о мерче (так как его всего 10 видов)

Это позволит существенно ускорить выполнение методов по переводу монет и покупке мерча, так как часто запрашиваемые данные будут извлекаться из памяти, а не из базы данных. Такой подход снизит нагрузку на PostgreSQL и улучшит масштабируемость сервиса.




